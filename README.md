#A webApp made using react-split.

For Precily. 
The app does not use any api calls or a database. Instead it uses the useState react Hook to display the number of times the text was changed and to update the etxt. 